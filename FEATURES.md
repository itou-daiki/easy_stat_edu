# easyStat 機能仕様

この文書は、トップ画面に表示される23機能と、全機能に共通する入力・表示仕様をまとめたものです。

## 共通仕様

### データ入力

- Excel (`.xlsx`, `.xls`) とCSVを読み込めます。
- ファイルがなくても、行列型の表へ直接入力できます。
- Excelやスプレッドシートから、見出しを含む範囲をそのまま貼り付けられます。
- 表入力では矢印キー、Enter、Tabでセルを移動できます。
- テキストマイニングは、表の文字列列または直接入力テキストを使用できます。

### 結果と可視化

- 各分析は、データ概要、設定、結果、解釈補助、図表の順を基本とします。
- Plotlyグラフ、Canvas図、結果表には共通の表示設定が付きます。
- タイトル、X/Y軸ラベル、凡例、幅、高さ、プリセットまたは「横:縦」の任意入力による縦横比を編集できます。項目を持たない図表では該当設定を表示しません。
- グラフとテキストマイニング画像を保存できます。表はコピーまたはCSV出力に対応する機能があります。
- p値の表示は`< .001`、有意記号は`** p<.01`、`* p<.05`、`† p<.10`、`n.s.`を共通規約とします。

## 1. 分析案内・データ準備

### 分析サポーター

目的、変数の型、群数、対応の有無などから候補となる分析を案内します。ルールベースの案内であり、任意のGemini解釈補助とは別機能です。

### データ加工・整形

列選択、欠損処理、外れ値処理、型変換、条件抽出、新しい列の作成など、分析前の整形を行います。

### データ結合

2つのExcel/CSVを、IDなどのキー列または行方向の追加で結合します。結合前にキーの重複や未対応行を確認します。

### 因子得点算出

複数項目の合計・平均を新しい得点列として作成します。逆転項目の指定と欠損の扱いを設定できます。

## 2. 記述と関連

### 探索的データ分析（EDA）

要約統計量、欠損、ヒストグラム、箱ひげ図、散布図などから分布と外れ値を確認します。標準偏差は標本標準偏差、歪度・尖度は標本数の条件を満たす場合にFisher調整値を表示します。

### クロス集計

2つのカテゴリ変数を度数、行%、列%、全体%で表示し、ヒートマップを作成します。2変数のどちらかが欠損する行は同時に除外し、有効ケース数と除外数を表示します。

### 相関分析

Pearson相関、Spearman順位相関、p値、信頼区間、相関行列、散布図を表示します。相関は因果関係を示しません。

## 3. 平均・順位の比較

### t検定

Welchの対応なしt検定、対応ありt検定、1標本t検定を実行します。標本数、平均、標準偏差、t、自由度、p値、Cohenの`d`または`d_z`を表示します。

### 一要因分散分析

独立群と反復測定に対応します。F検定、効果量、前提確認、多重比較を表示します。反復測定ではGreenhouse-Geisser補正を扱います。

### 二要因分散分析

独立、反復測定、混合計画の主効果と交互作用を分析します。研究デザインに合うモードと被験者IDを指定する必要があります。

### マン・ホイットニーのU検定

独立2群を順位で比較し、U、`|Z|`、p値、効果量`r`を表示します。分布形状が異なる場合、結果を単純な中央値差とみなさない注意が必要です。

### クラスカル・ウォリス検定

独立3群以上を順位で比較します。H、タイ補正、p値、効果量と、必要に応じて多重比較を表示します。

### ウィルコクソンの符号付順位検定

対応のある2時点・2条件を順位で比較します。ゼロ差を除外し、正規近似では連続性補正とタイ補正を扱います。

## 4. カテゴリデータの検定

### マクネマー検定

同じ対象の2時点における同一2カテゴリの比率変化を、不一致ペアから検定します。不一致ペアが少ない場合は正確二項検定を主結果とします。

### カイ二乗検定

独立性のPearsonカイ二乗検定、CramerのV、調整済み標準化残差を表示します。2×2表ではYatesの連続性補正を主結果とし、補正なしPearson値を参考表示します。R×C表のセルp値はHolm補正し、未補正でのみ目立つセルと区別します。期待度数1未満、または5未満セルが20%を超える場合は警告します。

### フィッシャーの正確確率検定

2×2表は超幾何分布により両側確率を計算します。R×C表は規模に応じて全表列挙または条件付きモンテカルロ推定を使用し、採用方法を結果に表示します。

## 5. 回帰

### 単回帰分析

1つの説明変数と目的変数からOLS回帰を求め、切片・傾きのSE、t、p、信頼区間、R²、残差を表示します。

### 重回帰分析

複数の説明変数を含むOLS回帰を求めます。偏回帰係数、標準化係数、モデルF検定、R²、調整済みR²、VIF、残差診断を表示します。

### ロジスティック回帰分析

2値の目的変数を、1つ以上の説明変数から予測します。係数、オッズ比、信頼区間、擬似R²、標本内の分類表を表示します。汎化性能の判断には別データまたは交差検証が必要です。

回帰係数は関連・予測の指標です。観察データの回帰だけから因果効果を断定できません。

## 6. 多変量・時系列

### 因子分析

複数の数値変数から共通因子を探索します。因子数、共通性、因子負荷量、回転（varimax、promax、oblimin、geomin、なし）、因子得点を扱います。

### 主成分分析（PCA）

標準化した数値変数を固有値分解し、主成分得点、主成分負荷量、寄与率、累積寄与率、スクリープロット、バイプロットを表示します。

### 時系列データ分析

時間順の数値データについて、系列グラフ、移動平均、自己相関などを確認します。独立観測を前提とする通常の検定とはデータ順序の扱いが異なります。

## 7. テキストマイニング

- 表データの文字列列または直接入力を分析します。
- 軽量な日本語分割処理と品詞推定をブラウザ内で実行します。
- 出現頻度、文書頻度、TF-IDF、品詞別ランキングを表示します。
- 頻度ワードクラウドとTF-IDFワードクラウドを並べ、語の色は品詞、語の大きさは各指標を表します。
- KWICで選択語の前後文脈を確認できます。
- 共起ネットワークは文単位の共起とJaccard係数を使い、ノード選択からKWICへ移動できます。
- カテゴリ列を指定すると、カテゴリごとのワードクラウドと共起ネットワークを連続表示し、群間の特徴語を比較します。
- ワードクラウドと共起ネットワークは画像保存できます。

KH Coderと分析目的が重なる部分はありますが、easyStatはGitHub Pages上で動く軽量実装です。形態素解析器、辞書、コーディング機能、統計モデルが異なるため、同一結果を保証しません。厳密な研究利用では、原文確認、辞書・ストップワードの記録、別手法との照合を行ってください。

## 制限と注意

- 大規模データ、R×C表の列挙、多数カテゴリのテキスト可視化は端末性能の影響を受けます。
- JavaScriptの倍精度浮動小数点を使用するため、他ソフトと丸めの最終桁が異なる場合があります。
- 非有意結果は、差・関連がないことの証明ではありません。
- 自動解釈と任意のAI解釈は判断補助であり、研究計画や測定の妥当性を保証しません。
- 外部通信とデータ取扱いは[README.md](README.md)の説明を確認してください。

© 2022-2026 Dit-Lab. (Daiki Ito)
